# flyer_chat_video_message example

See the [package README](../README.md) for full usage and customisation examples.

To run a complete demo, wire `FlyerChatVideoMessage` into a `flutter_chat_ui`
`Chat` widget via `Builders.videoMessageBuilder` as shown in the README.
