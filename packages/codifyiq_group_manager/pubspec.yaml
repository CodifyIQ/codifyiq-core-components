name: codifyiq_group_manager
description: >-
  Material 3 widgets for managing flat authorization groups, worked from either end:
  assign groups to a user, or add members to a group.
version: 1.3.0
homepage: https://github.com/CodifyIQ/codifyiq-core-components/tree/dev/packages/codifyiq_group_manager
repository: https://github.com/CodifyIQ/codifyiq-core-components/tree/dev/packages/codifyiq_group_manager
issue_tracker: https://github.com/CodifyIQ/codifyiq-core-components/issues
topics:
  - codifyiq
  - ui
  - widgets
  - authorization

resolution: workspace

environment:
  sdk: ^3.10.0
  flutter: ">=3.41.0"

dependencies:
  codifyiq_user_avatar: ^1.1.1
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
